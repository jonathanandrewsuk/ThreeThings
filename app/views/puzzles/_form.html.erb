<%= form_with(model: puzzle, local: true) do |form| %>
  <% if puzzle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(puzzle.errors.count, "error") %> prohibited this puzzle from being saved:</h2>

      <ul>
      <% puzzle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form_for :puzzle do |f| %>
  img_url1: <%= f.text_field :img_url1 %><br />
  img_url2: <%= f.text_field :img_url2 %><br />
  img_url3: <%= f.text_field :img_url3 %><br />
  phrase: <%= f.text_field :phrase %><br />
  <%= f.submit %>
<% end %>
  </div>
<% end %>
